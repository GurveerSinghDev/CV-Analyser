# CV-Analyser
Analyses CVs 

# m- venv venv (creates VE)
# .\venv\Scripts\Activate 
# enter in terminal, to enter VE
# 'dactivate' to Exit
# Download stuff ‘pip install -r requirements.txt’ 
# Remove-Item -Recurse -Force venv (removes VE)
# python -m venv venv (reintall VE)
#  python app/pdfParser.py app/testCvs/John_Doe_CV.pdf (test run extraction)

# FastApi
# pip install fastapi uvicorn python-multipart
# Run api, u do this 'uvicorn app.main:app --reload'
# http://127.0.0.1:8000/docs ,to test pdf upload

# React
 # Checks if you have node: node –v or npm –v 
# Friends do 'npm install' after cloning my repo 
# npm start, runs the page
# ctrl + c to quit

 

